# Backend para el sistema de inventario

Este es el backend del sistema de inventario para la gestión de productos, proveedores y stock en una tienda de ropa.

## Configuración

1. Instalar dependencias: `pip install -r requirements.txt`
2. Ejecutar el servidor: `uvicorn main:app --reload`
